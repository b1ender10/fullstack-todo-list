# 🔄 Система миграций БД

Система миграций для управления изменениями схемы базы данных с версионированием.

## 📋 Возможности

- ✅ Автоматическое применение всех pending миграций
- ✅ Откат последней миграции
- ✅ Отслеживание примененных миграций в таблице `schema_migrations`
- ✅ Просмотр статуса миграций
- ✅ Версионирование схемы БД

## 🚀 Использование

### Применить все pending миграции
```bash
npm run migrate
# или
npm run migrate:up
```

### Откатить последнюю миграцию
```bash
npm run migrate:down
```

### Показать статус миграций
```bash
npm run migrate:status
```

## 📁 Структура

- `migrations/` - папка с миграциями
- `migrate.js` - скрипт для применения миграций
- `schema_migrations` - таблица в БД для отслеживания примененных миграций

## 🔧 Как работает

1. **Таблица версионирования**: При первом запуске создается таблица `schema_migrations`, которая хранит список примененных миграций.

2. **Сканирование миграций**: Скрипт сканирует папку `migrations/` и находит все файлы с паттерном `NNN_description.js`.

3. **Применение**: Применяются только те миграции, которых нет в таблице `schema_migrations`, в порядке возрастания номера.

4. **Откат**: Откатывается последняя примененная миграция (в обратном порядке).

## 📝 Формат миграций

Каждая миграция должна:
- Иметь имя файла: `NNN_description.js` (где NNN - номер по порядку)
- Экспортировать функцию `up()` - применяет изменения
- Экспортировать функцию `down()` - откатывает изменения

Пример:
```javascript
export async function up() {
  await db.run('ALTER TABLE todos ADD COLUMN new_field TEXT');
}

export async function down() {
  // Откат изменений
}
```

## 🎯 Примеры

### Применить все миграции
```bash
$ npm run migrate
📦 Найдено 3 миграций для применения:

▶️  Применяем миграцию: 001_add_due_date_to_todos.js
✅ Миграция 001_add_due_date_to_todos.js применена

▶️  Применяем миграцию: 002_add_indexes.js
✅ Миграция 002_add_indexes.js применена

✅ Все миграции применены (3)
```

### Проверить статус
```bash
$ npm run migrate:status
📊 Статус миграций:

  ✅ Применена - 001_add_due_date_to_todos.js
  ✅ Применена - 002_add_indexes.js
  ⏳ Ожидает - 003_add_date_expiration.js

📈 Всего: 3, Применено: 2, Ожидает: 1
```

## ⚠️ Важно

- Не изменяйте уже примененные миграции
- Создавайте новую миграцию для новых изменений
- Всегда тестируйте миграции перед применением в production
- Делайте backup БД перед применением миграций в production

